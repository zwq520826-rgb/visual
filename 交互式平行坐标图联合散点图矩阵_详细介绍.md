# 视图3：交互式平行坐标图联合散点图矩阵

## 一、视图概述

**视图3：交互式平行坐标图联合散点图矩阵**是一个参考 FLINAView 设计理念的多视图协同可视化系统，用于在宏观层面展现城市、经验要求、薪资水平、公司类型与岗位多样性之间的综合关系。

该视图通过**平行坐标图**和**散点图矩阵**两个互补的可视化组件，实现了多维度数据的综合表达，使用户能够在单一图形中直观观察不同经验层次下各城市招聘数量、薪资水平及岗位多样性之间的关系，并通过丰富的交互操作深入探索数据模式。

---

## 二、核心功能特性

### 2.1 多视图协同可视化

- **平行坐标图**：展示6个维度的完整关系，每个数据点用一条折线连接所有维度
- **散点图矩阵**：展示关键维度对的分布关系，包括3个核心散点图
- **视图联动**：所有视图同步高亮和过滤，实现真正的多视图协同分析

### 2.2 丰富的交互操作

- **拖动刷选**：在平行坐标图的任意轴上拖动选择数据范围
- **框选数据点**：在散点图上框选多个数据点
- **点击选择**：点击单个数据点进行精确选择
- **清除选择**：一键清除所有筛选条件
- **鼠标悬停**：查看数据点的详细信息

### 2.3 灵活的可视化配置

- **颜色映射**：支持按薪资、岗位多样性、岗位数量三种方式映射线条颜色
- **透明度调节**：可调节线条透明度（0.1-1.0），避免重叠线条遮挡
- **显示模式**：支持全部数据、前200条、前100条三种显示模式

---

## 三、数据维度说明

### 3.1 六个核心维度

| 维度名称 | 数据类型 | 说明 | 取值范围 |
|---------|---------|------|---------|
| **城市 (city)** | 分类数据 | 招聘职位所在城市 | 字符串（如：F379, Y232等） |
| **工作经验 (experience)** | 分类数据 | 职位要求的工作经验 | 已映射为中文标签（如：1-3年、3-5年等） |
| **学历要求 (education)** | 分类数据 | 职位要求的学历水平 | 已映射为中文标签（如：本科、硕士、博士等） |
| **公司类型 (company_type)** | 分类数据 | 招聘公司的类型 | 字符串（如：上市公司、民营公司等） |
| **平均薪资 (salary)** | 数值数据 | 该组合下的平均薪资水平 | 浮点数（单位：K，即千元） |
| **岗位多样性 (entropy)** | 数值数据 | 岗位类型的多样性程度 | 浮点数（0-1范围，使用香农熵计算） |

### 3.2 岗位多样性计算

岗位多样性使用**香农熵（Shannon Entropy）**进行计算：

```
H(X) = -Σ p(x) log₂ p(x)
```

其中：
- `p(x)` 表示岗位类型 x 在该数据组合中的概率
- 熵值越高表示岗位类型越多样，颜色越深
- 熵值范围：0-1（已归一化）

### 3.3 数据映射说明

- **经验和学历字段**：已通过数据库映射表（`experience_mapping`、`education_mapping`）转换为可读的中文标签
- **数据聚合**：数据按城市、经验、学历、公司类型进行分组聚合
- **数据限制**：默认返回前1000条数据，支持前端进一步筛选

---

## 四、可视化组件详解

### 4.1 平行坐标图（左侧主视图）

#### 4.1.1 布局结构

- **垂直轴**：6个维度轴垂直排列，从左到右依次为：城市、工作经验、学历要求、公司类型、平均薪资、岗位多样性
- **数据线**：每个数据点用一条折线连接所有维度，折线在对应维度轴上的位置表示该数据点在该维度上的取值
- **轴标签**：每个轴顶部显示维度名称，底部显示数值范围或分类值

#### 4.1.2 交互功能

1. **拖动刷选（Brushing）**
   - 在任意维度轴上拖动鼠标，选择该维度上的数值范围
   - 被选中的数据线会高亮显示，未选中的数据线会变暗
   - 支持在多个轴上同时进行刷选，实现多维度联合筛选

2. **鼠标悬停（Hover）**
   - 鼠标悬停在数据线上时，显示该数据点的详细信息
   - 信息包括：城市、经验、学历、公司类型、薪资、岗位多样性、岗位数量

3. **点击选择**
   - 点击数据线可以选择单个数据点
   - 选中的数据点会在所有视图中同步高亮

#### 4.1.3 视觉编码

- **线条颜色**：根据配置的颜色映射（薪资/岗位多样性/岗位数量）进行着色
- **线条粗细**：统一粗细，通过颜色和透明度区分
- **线条透明度**：可调节，默认0.3，避免重叠线条遮挡
- **高亮效果**：选中的数据线加粗并提高不透明度

### 4.2 散点图矩阵（右侧辅助视图）

#### 4.2.1 三个核心散点图

1. **薪资 vs 岗位多样性**
   - X轴：平均薪资（单位：K）
   - Y轴：岗位多样性（香农熵值，0-1范围）
   - 用途：观察薪资水平与岗位多样性的关系

2. **薪资 vs 岗位数量**
   - X轴：平均薪资（单位：K）
   - Y轴：岗位数量（整数）
   - 用途：观察薪资水平与招聘规模的关系

3. **岗位多样性 vs 岗位数量**
   - X轴：岗位多样性（香农熵值，0-1范围）
   - Y轴：岗位数量（整数）
   - 用途：观察岗位多样性与招聘规模的关系

#### 4.2.2 交互功能

1. **框选（Brush Selection）**
   - 在散点图上拖动鼠标框选数据点
   - 框选的数据点会在平行坐标图中同步高亮
   - 支持多个散点图同时框选

2. **点击选择**
   - 点击单个数据点进行精确选择
   - 选中的数据点会在所有视图中同步高亮

3. **鼠标悬停**
   - 鼠标悬停在数据点上时，显示详细信息
   - 信息包括：城市、经验、学历、公司类型、薪资、岗位多样性、岗位数量

#### 4.2.3 视觉编码

- **点的大小**：根据岗位数量进行映射，岗位越多点越大
- **点的颜色**：与平行坐标图的颜色映射保持一致
- **点的透明度**：未选中的数据点降低透明度
- **高亮效果**：选中的数据点加粗边框并提高不透明度

---

## 五、控制面板功能

### 5.1 颜色映射选择

- **按薪资映射**：线条颜色反映薪资水平，薪资越高颜色越深（红色系）
- **按岗位多样性映射**：线条颜色反映岗位多样性，多样性越高颜色越深（蓝色系）
- **按岗位数量映射**：线条颜色反映岗位数量，数量越多颜色越深（绿色系）

### 5.2 线条透明度调节

- **范围**：0.1（最透明）到 1.0（完全不透明）
- **步长**：0.1
- **用途**：当数据点较多时，降低透明度可以避免重叠线条遮挡，更好地观察数据分布

### 5.3 显示模式选择

- **全部数据**：显示所有数据点（最多1000条）
- **前200条**：仅显示前200条数据，提高渲染性能
- **前100条**：仅显示前100条数据，用于快速预览

### 5.4 清除选择按钮

- **功能**：一键清除所有筛选条件
- **效果**：所有视图恢复到初始状态，显示所有数据

### 5.5 选择状态显示

- **已选择数量**：显示当前选中的数据点数量
- **总数据量**：显示当前显示的数据点总数
- **格式**：`已选择: X / Y`

---

## 六、视图联动机制

### 6.1 同步高亮

- 在平行坐标图上刷选的数据，会自动在散点图矩阵中高亮对应的数据点
- 在散点图上框选的数据，会自动在平行坐标图中高亮对应的数据线
- 所有视图的高亮状态保持同步

### 6.2 同步过滤

- 在平行坐标图上刷选后，散点图矩阵会自动过滤，只显示被选中的数据点
- 在散点图上框选后，平行坐标图会自动过滤，只显示被选中的数据线
- 未选中的数据会变暗或隐藏

### 6.3 信息同步

- 鼠标悬停时，所有视图同步显示对应数据点的详细信息
- 选中的数据点信息会在控制面板中显示

---

## 七、使用场景

### 7.1 探索性数据分析

- **发现数据模式**：通过平行坐标图观察多维度数据的整体分布模式
- **识别异常值**：通过散点图矩阵快速识别异常的数据点
- **相关性分析**：通过散点图观察不同维度之间的相关性

### 7.2 多维度筛选

- **精确筛选**：通过平行坐标图的拖动刷选，精确选择符合多个条件的数据
- **快速筛选**：通过散点图的框选，快速选择特定范围的数据
- **组合筛选**：结合多个视图的筛选条件，实现复杂的多维度筛选

### 7.3 数据对比分析

- **城市对比**：对比不同城市在薪资、岗位多样性等维度上的差异
- **经验层次对比**：对比不同经验要求下的薪资和岗位分布
- **公司类型对比**：对比不同公司类型在招聘特征上的差异

### 7.4 趋势识别

- **薪资趋势**：观察不同维度组合下的薪资水平趋势
- **多样性趋势**：观察岗位多样性的变化趋势
- **规模趋势**：观察招聘规模的变化趋势

---

## 八、技术实现细节

### 8.1 前端技术栈

- **框架**：Vue 3（Composition API）
- **可视化库**：D3.js v7
- **响应式设计**：使用 `ResizeObserver` 实现自适应布局
- **动画效果**：使用 D3.js 的 `transition` API 实现平滑动画

### 8.2 核心算法

1. **平行坐标图渲染**
   - 使用 D3.js 的 `scalePoint` 和 `scaleLinear` 创建比例尺
   - 使用 `d3.line()` 创建折线生成器
   - 使用 `d3.brush` 实现拖动刷选功能

2. **散点图渲染**
   - 使用 D3.js 的 `scaleLinear` 创建坐标轴
   - 使用 `d3.brush` 实现框选功能
   - 使用 `d3.forceSimulation` 实现数据点的力导向布局（可选）

3. **视图联动**
   - 使用 Vue 3 的 `ref` 和 `watch` 实现状态同步
   - 使用事件总线实现跨组件通信
   - 使用 D3.js 的选择器实现批量样式更新

### 8.3 性能优化

- **数据限制**：默认限制显示1000条数据，避免性能问题
- **虚拟滚动**：对于大量数据，使用虚拟滚动技术
- **防抖处理**：对频繁的交互操作进行防抖处理
- **按需渲染**：只渲染可见区域的数据点

### 8.4 动画效果

- **初始动画**：图表加载时，从左到右缓慢显示
   - 轴扩展动画：轴从左侧逐渐展开
   - 标签淡入：轴标签逐渐显示
   - 线条绘制：数据线使用 `stroke-dasharray` 动画效果
   - 散点增长：散点从小到大逐渐显示

- **交互动画**：所有交互操作都有平滑的过渡效果
   - 高亮过渡：数据点/线的颜色和透明度平滑过渡
   - 筛选过渡：数据点的显示/隐藏有淡入淡出效果

---

## 九、API接口说明

### 9.1 接口信息

- **接口路径**：`/api/charts/parallel-coordinates`
- **请求方法**：`GET`
- **请求参数**：无

### 9.2 响应数据格式

```json
{
  "status": "success",
  "code": 200,
  "message": "获取平行坐标图数据成功",
  "data": {
    "data": [
      {
        "city": "F379",
        "experience": "1-3年",
        "education": "本科",
        "company_type": "上市公司",
        "salary": 15.5,
        "entropy": 4.9069,
        "job_count": 1250
      },
      ...
    ]
  }
}
```

### 9.3 数据字段说明

| 字段名 | 类型 | 说明 |
|--------|------|------|
| `city` | string | 城市名称 |
| `experience` | string | 工作经验要求（已映射为中文） |
| `education` | string | 学历要求（已映射为中文） |
| `company_type` | string | 公司类型 |
| `salary` | float | 平均薪资（单位：K） |
| `entropy` | float | 岗位多样性（香农熵值） |
| `job_count` | integer | 职位数量 |

---

## 十、使用指南

### 10.1 基本操作流程

1. **加载数据**：切换到Q3标签页，视图3会自动加载数据
2. **观察整体**：首先观察平行坐标图的整体分布，了解数据的大致模式
3. **选择维度**：根据分析目标，选择合适的颜色映射方式
4. **进行筛选**：在平行坐标图上拖动刷选，或在散点图上框选数据点
5. **查看详情**：鼠标悬停在数据点/线上，查看详细信息
6. **清除筛选**：点击"清除选择"按钮，重置所有筛选条件

### 10.2 高级技巧

1. **多维度联合筛选**：在平行坐标图的多个轴上同时进行刷选，实现精确的多维度筛选
2. **组合视图筛选**：先在平行坐标图上筛选，再在散点图上进一步筛选，实现更精确的分析
3. **透明度调节**：当数据点较多时，降低透明度可以更好地观察数据分布
4. **显示模式切换**：对于性能较差的设备，可以切换到"前200条"或"前100条"模式

### 10.3 常见问题

**Q: 为什么有些数据线看不到？**  
A: 可能是数据点太多导致重叠，可以降低透明度或切换到"前200条"模式。

**Q: 如何精确选择特定城市的数据？**  
A: 在平行坐标图的"城市"轴上拖动刷选，选择对应的城市范围。

**Q: 如何对比不同经验层次的数据？**  
A: 在平行坐标图的"工作经验"轴上分别刷选不同的经验范围，观察数据线的分布差异。

**Q: 散点图上的点太大/太小怎么办？**  
A: 点的大小是根据岗位数量自动计算的，这是为了更直观地展示数据规模。

---

## 十一、设计理念

### 11.1 FLINAView 参考

本视图参考了 FLINAView（Fast Linked Interactive Analysis View）的设计理念：

- **多视图协同**：多个视图同时展示数据的不同方面，实现互补分析
- **交互联动**：所有视图的交互操作相互联动，实现真正的协同分析
- **快速响应**：所有交互操作都有即时反馈，提供流畅的用户体验

### 11.2 信息可视化原则

- **多维度展示**：通过平行坐标图同时展示6个维度，避免维度灾难
- **互补视图**：平行坐标图和散点图矩阵互补，提供不同角度的数据洞察
- **交互式探索**：通过丰富的交互操作，支持用户主动探索数据

### 11.3 用户体验优化

- **渐进式展示**：初始动画让用户逐步理解图表结构
- **即时反馈**：所有交互操作都有即时的视觉反馈
- **灵活配置**：提供多种可视化配置选项，满足不同用户需求

---

## 十二、总结

**视图3：交互式平行坐标图联合散点图矩阵**是一个功能强大、交互丰富的多视图协同可视化系统。它通过平行坐标图和散点图矩阵两个互补的可视化组件，实现了多维度数据的综合表达，使用户能够在单一图形中直观观察不同经验层次下各城市招聘数量、薪资水平及岗位多样性之间的关系。

通过丰富的交互操作（拖动刷选、框选、点击选择等）和视图联动机制，用户可以深入探索数据模式，发现隐藏的数据规律，为人才招聘和求职决策提供可行的建议。

该视图实现了多维度信息的综合表达，使用户能够在单一图形中直观观察不同经验层次下各城市招聘数量、薪资水平及岗位多样性之间的关系，并通过交互式筛选深入探索数据模式。

---

**文档版本**：v1.0  
**最后更新**：2025-01-XX  
**技术支持**：如有问题或建议，请联系开发团队


